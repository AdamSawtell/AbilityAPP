# Tasks — all

| Field | Value |
|-------|-------|
| **ID** | `tasks-all` |
| **Status** | Live |
| **Module** | Core (Tasks) |
| **Max columns** | 20 |

Full task listing across all statuses. Requires `tasks-all` window access. See `web/src/lib/reports/runners/tasks-all.ts`.
